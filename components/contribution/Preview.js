import ImageCard from '../discover/ImageCard';

const Preview = ({ formData }) => {
  return (
    <div>
      <ImageCard
        workshop={{
          ID: 'temporary',
        }}
      ></ImageCard>
    </div>
  );
};

export default Preview;
